// ETF overlap UI removed — placeholder to avoid import errors.
console.info('etf-overlap-ui.js entirely replaced — ETF feature removed');
// No runtime behavior. Restore original implementation to re-enable the feature.
